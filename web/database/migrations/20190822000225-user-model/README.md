# Migration `20190822000225-user-model`

This migration has been generated by Max Stoiber at 8/22/2019, 12:02:25 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User"("id" text NOT NULL  ,"name" text NOT NULL DEFAULT '' ,"googleId" text NOT NULL DEFAULT '' ,"email" text   ,"avatarUrl" text   ,PRIMARY KEY ("id"));

CREATE UNIQUE INDEX "User.googleId._UNIQUE" ON "public"."User"("googleId")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190822000148-init..20190822000225-user-model
--- datamodel.dml
+++ datamodel.dml
@@ -4,9 +4,9 @@
 }
 model User {
   id String @id @default(cuid())
-  // name String
-  // googleId String @unique
-  // email String?
-  // avatarUrl String?
+  name String
+  googleId String @unique
+  email String?
+  avatarUrl String?
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190822000225-user-model)
in your `before` or `after` migration script like this:

```ts
import Photon from "@generated/photon/20190822000225-user-model";

const photon = new Photon();

async function main() {
  const result = await photon.users();
  console.dir(result, { depth: null });
}

main();
```
