# Migration `20190822000148-init`

This migration has been generated by Max Stoiber at 8/22/2019, 12:01:48 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User"("id" text NOT NULL  ,PRIMARY KEY ("id"));
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190822000148-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,12 @@
+datasource db {
+  provider = "postgresql"
+  url      = "postgresql://mxstbr@localhost:5432?schema=public"
+}
+
+model User {
+  id String @id @default(cuid())
+  // name String
+  // googleId String @unique
+  // email String?
+  // avatarUrl String?
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190822000148-init)
in your `before` or `after` migration script like this:

```ts
import Photon from "@generated/photon/20190822000148-init";

const photon = new Photon();

async function main() {
  const result = await photon.users();
  console.dir(result, { depth: null });
}

main();
```
